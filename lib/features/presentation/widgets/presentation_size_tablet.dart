part of '../portfolio_page.dart';

class PresentationSizeTablet extends StatelessWidget {
  const PresentationSizeTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
